
import { SharedLayout } from "@/components/shared-layout";
import { DashboardContent } from "@/components/dashboard-content";

const Index = () => {
  return (
    <SharedLayout breadcrumb="Student Login">
      <DashboardContent />
    </SharedLayout>
  );
};

export default Index;
